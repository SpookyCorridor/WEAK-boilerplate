import ConfirmClick from './confirm-click';
import SampleDirectives from './test';

export default angular.module('Directives', [

])
    .directive('confirm-click', ConfirmClick)
    .directive('firstDirective', SampleDirectives)
    .name; 